class Cat
  attr_accessor :breed, :color
  def initialize(breed, color)
      @breed = breed
      @color = color
  end
end

cat = Cat.new(:DMH, :orange)
cat.breed = :DSH
puts cat
puts cat.breed
puts cat.color
