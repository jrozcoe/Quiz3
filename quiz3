class Cat
  attr_accessor :breed, :color
  def initialize
      @breed = breed
      @color = color
  end
  
  def breed=(breed)
      @breed = breed
  end
end

cat = Cat.new(:DMH, :orange)
cat.breed = :DMH
puts cat
puts cat.breed
puts cat.color
